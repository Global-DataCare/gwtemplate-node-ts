interface:
  display_name: "Configure trusted identity providers"
  short_description: "Configure exact OIDC issuer and audience trust in one GW."
  default_prompt: "Configure or audit trusted OIDC identity providers for this GW deployment."
