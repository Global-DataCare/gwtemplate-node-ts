# Portal Web API -> GW CORE (referencia histórica v1.5)

Esta tabla ya no es un contrato operativo independiente.

La fuente única mantenida es
[`docs/PORTAL_API_TO_GW_CORE.md`](docs/PORTAL_API_TO_GW_CORE.md). La historia Git
de este archivo conserva la captura completa de v1.5 para auditoría. No copies
su inventario a un portal nuevo ni la uses para decidir si una ruta sigue
publicada; en caso de contradicción prevalecen la guía canónica, OpenAPI, los
tests de rutas y el SDK actual.
