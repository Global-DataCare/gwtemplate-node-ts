Title: Hosting Bootstrap: Fabric License, Enrollment, and Host VC

Status: Draft

Purpose
This guide explains the target hosting-operator bootstrap contract after the
old "single ICA CSR issuance" model was split into two different trust
domains:

- Fabric ICA for operational Fabric bootstrap
- dataspace ICA for host accreditation VC issuance

Scope
- Test network onboarding (`test-network`).
- Hosting operator operational certificate flow.
- Hosted tenant ledger-registration flow.
- Evidence is a signed PDF (PAdES) of Terms & Conditions.

## Flow Position

This document covers only the hosting bootstrap slice of the broader GW
onboarding/runtime lifecycle.

The larger route order is:

1. hosting candidate is validated against space requirements
2. adhesion is formalized in a signed PDF
3. dataspace ICA verifies the PDF and issues a signed
   `HostingServiceCredential`
4. the Root controller signs a governance decision binding that Host VC to an
   MSP, peer target, allowed channels and read/write policy
5. the privileged infrastructure reconciler verifies both signatures, updates
   channel membership and registers bounded Fabric CA enrollment identities
6. the host controller receives the one-time enrollment material
7. the host runs `fabric-ca-client enroll` locally; its MSP and TLS private
   keys and CSRs never leave the host
8. the reconciler joins the authorized peer to the approved channels and
   applies the required chaincode lifecycle
9. host autodiscovery publishes only operational provider-capable tenants

`Token/_exchange` and `Device/_dcr` are not certificate issuance steps. They
bootstrap the app/device technical identity used to call the tenant runtime.
Fabric enrollment is a host trust artifact, not a per-tenant Fabric
certificate flow, and Host VC issuance belongs to dataspace ICA, not to Fabric
CA.

## Canonical Trust Separation

### A. Hosting compliance and adhesion

- Validate that the hosting operator meets the data-space requirements.
- Only after validation, formalize the adhesion in a signed PDF.
- This step produces the evidence package that later feeds both ICA flows.

### B. Dataspace ICA host accreditation

- Input: validated hosting context plus signed adhesion PDF.
- Output: a signed schema.org `HostingServiceCredential`.
- The credential identifies the approved host service; it is not a Fabric
  private key, enrollment certificate or channel-membership decision.

### C. Root governance and Fabric CA enrollment

- Input: the Host VC plus a Root-controller governance decision.
- Output: reconciled channel membership and bounded Fabric CA enrollment
  identities for MSP and TLS.
- Fabric CA issues certificates but never generates or exports the host private
  keys.

1) Key + CSR generation (local)
- The private key is generated locally inside the gateway/KMS. It is NEVER uploaded.
- A CSR is built locally from that key pair.
- The CN for the cert uses the service name (alternateName), not the legal name.

2) Governed registration and local enrollment
- `fabric-multicloud` verifies the Host VC against an allowlisted dataspace ICA
  DID and verifies the Root governance JWS.
- The registration step creates bounded one-time Fabric CA identities.
- The host executes enrollment locally twice, for MSP and TLS, so
  `fabric-ca-client` creates the private keys and CSRs on the host.

Publication rule:

- Fabric ICA issuing a host certificate does not by itself make hosted tenants publicly
  discoverable.
- Public discovery remains a host-side GW publication step derived from the
  tenant operational state and service capabilities.

Operational implementation

- `Fundacion-UNID/fabric-multicloud` owns Root authorization verification,
  declarative channel reconciliation, Fabric CA registration and the host-side
  enrollment script.
- GW Core does not expose or call a demo `_enroll` endpoint and must never send
  `DEMO-CSR`, `Bearer demo` or a host private key.
- `authorize-host-enrollment.mjs` verifies the Host VC, trusted ICA issuer,
  Root governance decision and requested MSP/peer binding.
- `register-host-enrollment.sh` registers bounded MSP/TLS identities.
- `enroll-host-msp.sh` runs on the host and refuses to overwrite existing key
  material.

Messaging endpoints (DIDComm v2)
Base path:
`/<tenant_id>/cds-<jurisdiction>/v1/<sector>/messaging/<format>/<resourceType>/<action>`

Example (post-quantum, send):
`/host/cds-es/v1/system/messaging/post-quantum/didcomm-encrypted/_send`

SDK guidance
- Preferred: applications perform DIDComm encrypt/sign/verify **locally** via SDK.
- Server endpoints exist as a **fallback** for systems without SDK support.

Important certificate boundary

- A tenant organization X.509 leaf chains to the dataspace ICA organization
  certification CA (`CA:TRUE, pathLen=0`).
- A host peer MSP/TLS certificate chains to Fabric CA.
- Neither certificate is interchangeable with the ES384 `CA:FALSE` certificate
  used by dataspace ICA to sign VCs.

4) Gateway publishes host cert (public)
The gateway exposes:
- `/.well-known/jwks.json` with `x5c` (and optionally `x5u`)
- `/.well-known/x509.der` (certificate chain)

This publication is for the host/operator identity. The host-level service
autodiscovery surface remains separate:

- `GET /host/cds-{jurisdiction}/{version}/{hostNetwork}/.well-known/dspace-version`
- `POST /api/dataspace-discovery/providers`

Those endpoints publish provider availability, not certificate issuance state.

TODO:
- ICA should align its hosting-operator discovery guidance with the same
  `/.well-known/dspace-version` entrypoint model used by GW CORE host and tenant
  DSP publication, instead of relying on a separate host-level `well-known`
  catalog artifact.
- For ICA and host/runtime technical paths, the segment after
  `cds-{jurisdiction}/{version}` must be interpreted as `hostNetwork`
  (`test`, `test-network`, `network`), not as a
  tenant business sector.

5) Hosted tenant ledger registration (organization-sc)
When `LEDGER_ENABLED=true`, the gateway registers hosted tenant organizations on
the ledger after successful onboarding (Order confirmation).

Environment
- `LEDGER_ENABLED=true`
- `LEDGER_MSP_ID=UNIDMSP` (or the MSP that submits the transaction)
- `LEDGER_ORG_CHAINCODE=organization-sc` (optional override)
- `GOVERNANCE_SCHEMA_URL=https://governance.example.org/schema/organization.jsonld`

Channel selection
- Identity channel is derived by jurisdiction: `eu-identity`, `na-identity`, etc.

Payload fields (organization-sc)
- `orgId`: organization identifier (URN)
- `schemaUrl`: governance schema URL
- `governanceVc`: legal-participant VC
- `selfDescriptionVc`: self-description VC
- `keys`: DID verificationMethod array (public keys)
- `evidence`: PDF signature evidence (see below)
- `metadata`: { role, sector, namespace, host, organization }

Evidence format (minimum)
The gateway extracts the PDF signature and stores the evidence as:
```
{
  "type": "DocumentSignature",
  "signature": {
    "type": "PAdES",
    "signatureValue": "<base64url-signature-bytes>"
  },
  "digest": [
    { "type": "DocumentHash", "hashAlg": "SHA256", "hashValue": "<base64url>" },
    { "type": "SignedDocumentHash", "hashAlg": "SHA256", "hashValue": "<base64url>" }
  ],
  "document": [
    { "type": "DigitalDocument", "hashAlg": "SHA256", "hashValue": "<base64url>" },
    { "type": "DigitalDocument", "hashAlg": "SHA256", "hashValue": "<base64url>" }
  ],
  "x5c": [ "<base64-der-cert>", "..." ]
}
```

Ledger conflict rule
If the same `(DocumentHash, SignedDocumentHash)` pair already exists in the
ledger, `organization-sc` returns `EvidenceAlreadyRegistered`, and the gateway
responds with HTTP 409.

Where this runs in code
- PDF evidence extraction: `src/utils/pdf-evidence.ts`
- Onboarding flow:
  - `HostingManager.processRegistrationEntry` stores evidence in the Service meta.
  - `HostingManager.processOrderEntry` registers the org on ledger.
  - `HostingManager.persistHostConfig` registers the host when enabled.

Notes
- For demo mode (`NODE_ENV=demo` or `NODE_ENV=test`), ledger registration is
  disabled unless `LEDGER_ENABLED=true`.
- The Fabric bootstrap endpoint still needs to evolve from simple CSR/evidence
  submission to an explicit operational-license bootstrap contract.
- The dataspace ICA Host VC exchange is a separate TODO and must not be folded
  into the Fabric enrollment call.
- Lifecycle TODO for `v1`:
  - tenant/employee/individual `disable` should be modeled as suspension, not deletion and not automatic license release.
  - authoritative credential suspension/revocation must come from ICA as a VC/ledger status update.
  - the host portal/GW should later support a canonical `/_deactivate` endpoint that ingests ICA-backed status updates and persists the corresponding suspended local state.

## Current Contract Summary

Use this separation when documenting or implementing the runtime:

- GW onboarding and activation make the tenant operational.
- `Token/_exchange` and `Device/_dcr` bind the technical client.
- Fabric enrollment signs the hosting operator certificate after operational
  licensing/bootstrap.
- dataspace ICA issues the Host VC after receiving the signed adhesion PDF plus
  the Fabric operational license/bootstrap evidence.
- Host autodiscovery decides whether the tenant is publicly published.

That keeps CSR issuance, device registration, and service autodiscovery from
collapsing into one undocumented step.
