---
alwaysApply: true
---

Before using the `edit_existing_file` tool, I MUST first use the `read_file` tool on the same file path. This ensures I have the absolute latest version of the file and prevents overwriting any recent changes made by the user.