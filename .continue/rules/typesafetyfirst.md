---
{}
---

After creating or editing any TypeScript file (`.ts`), I MUST run `npx tsc --noEmit` in the terminal to verify that I have not introduced any new compilation errors. I am strictly prohibited from showing the file to the user, running a test against it, or claiming a step is complete until this check passes.